# Zwischenspeicher der abgerufenen Seiten (nur lokal, 6 h gueltig)
.cache/

# players_raw.json.gz wird BEWUSST eingecheckt (0,7 MB gepackt). Ohne den
# Bestand im Repository startet die Action ohne Vorlage - ein Lauf mit nur
# einer Liga ueberschriebe dann alle uebrigen. Genau das ist einmal passiert.
data/players_raw.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
